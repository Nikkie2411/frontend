// App version for cache control
const APP_VERSION = 'v15';
const CACHE_TIMESTAMP = Date.now();

console.log(`🚀 PedMed App ${APP_VERSION} loaded at ${new Date().toLocaleString()}`);

// Check for updates
function checkForUpdates() {
    // This will be implemented later for update notifications
    console.log('🔄 Checking for updates...');
}